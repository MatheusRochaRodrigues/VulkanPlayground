// #pragma once
// #include <array>
// #include <engine_vulkan/vk_types.h>
// #include <engine_vulkan/vk_descriptors.h> 
// #include <utils/clean_behave.h>

/*
class VulkanCommands {
public:
    void init(VulkanContext& context);
    void shutdown(VulkanContext& context);

    FrameData& frame(uint32_t index);

    VkCommandBuffer immediateCommandBuffer() const;
    VkCommandPool immediateCommandPool() const;

private:
    std::array<FrameData, FRAME_OVERLAP> _frames;       //FrameData _frames[FRAME_OVERLAP]; 

    VkCommandPool _immCommandPool{};
    VkCommandBuffer _immCommandBuffer{};
};
*/
